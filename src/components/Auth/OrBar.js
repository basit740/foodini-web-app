import React from 'react';
import classes from '../../styles/auth/auth.module.css';
const OrBar = () => {
	return (
		<div className={classes.or_bar_section}>
			<h3 className={classes.or}>or</h3>
		</div>
	);
};

export default OrBar;
